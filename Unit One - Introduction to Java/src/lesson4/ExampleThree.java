package lesson4;

public class ExampleThree {
    //error types

    // syntax error - somethings java doesn't understand (misstyping)
        //int mark = 3(;)
    // logic error - it works but doesn't produce desired output (thinking wrong)
        int mark1 = 80, mark2 = 83, mark3 = 96;
        double average = mark1 + mark2 + mark3 / 3;
    // runtime error - program crashes while running
        int result = 3 / 0;
}
